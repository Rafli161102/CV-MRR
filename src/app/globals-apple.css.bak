/* stylelint-disable at-rule-no-unknown */
@tailwind base;
@tailwind components;
@tailwind utilities;
/* stylelint-enable at-rule-no-unknown */

/* ==========================================================================
   1. CUSTOM UTILITIES - Apple Style Dynamic Animations
   ========================================================================== */
@layer utilities {
  /* Apple-style spring animations */
  .anim-fade-in-up, .reveal {
    animation: fadeInUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    opacity: 0;
  }

  /* Smooth floating animation */
  .anim-float {
    animation: float 6s ease-in-out infinite;
  }

  /* Apple-style scale spring */
  .anim-scale-spring {
    animation: scaleSpring 0.6s cubic-bezier(0.34, 1.56, 0.64, 1) forwards;
  }

  /* Smooth blur fade in */
  .anim-blur-in {
    animation: blurIn 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
  }

  /* Slide in from left */
  .anim-slide-left {
    animation: slideLeft 0.7s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    opacity: 0;
  }

  /* Slide in from right */
  .anim-slide-right {
    animation: slideRight 0.7s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    opacity: 0;
  }

  /* Elastic bounce for interactive elements */
  .anim-bounce-soft {
    animation: bounceSoft 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55) forwards;
  }

  /* Shimmer gradient effect */
  .anim-shimmer {
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.1), transparent);
    background-size: 200% 100%;
    animation: shimmer 2s infinite;
  }

  /* Pulse glow for accents */
  .anim-pulse-glow {
    animation: pulseGlow 2s ease-in-out infinite;
  }

  /* Typing cursor effect */
  .anim-type {
    animation: typing 0.8s steps(20) forwards;
    overflow: hidden;
    white-space: nowrap;
  }

  /* Stagger delays - Apple style timing */
  .anim-delay-100, .stagger-1 { animation-delay: 100ms; }
  .anim-delay-200, .stagger-2 { animation-delay: 200ms; }
  .anim-delay-300, .stagger-3 { animation-delay: 300ms; }
  .anim-delay-400, .stagger-4 { animation-delay: 400ms; }
  .anim-delay-500, .stagger-5 { animation-delay: 500ms; }
  .stagger-6 { animation-delay: 600ms; }
  .stagger-7 { animation-delay: 700ms; }
  .stagger-8 { animation-delay: 800ms; }

  /* Glass morphism utilities */
  .glass {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(20px) saturate(180%);
    -webkit-backdrop-filter: blur(20px) saturate(180%);
  }

  .glass-strong {
    background: rgba(255, 255, 255, 0.08);
    backdrop-filter: blur(30px) saturate(200%);
    -webkit-backdrop-filter: blur(30px) saturate(200%);
  }

  /* Apple-style hover lift */
  .hover-lift {
    transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1), box-shadow 0.3s ease;
  }
  .hover-lift:hover {
    transform: translateY(-4px);
  }

  /* Smooth scrollbar */
  .scrollbar-smooth {
    scrollbar-width: thin;
    scrollbar-color: rgba(255,255,255,0.2) transparent;
  }
  .scrollbar-smooth::-webkit-scrollbar {
    width: 6px;
  }
  .scrollbar-smooth::-webkit-scrollbar-track {
    background: transparent;
  }
  .scrollbar-smooth::-webkit-scrollbar-thumb {
    background: rgba(255,255,255,0.2);
    border-radius: 3px;
  }
}

/* ==========================================================================
   2. KEYFRAMES (Rumus Pergerakan Matematika Animasi)
   ========================================================================== */
@keyframes fadeInUp {
  0% {
    opacity: 0;
    transform: translateY(30px) scale(0.95);
    filter: blur(4px);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
    filter: blur(0);
  }
}

@keyframes float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  25% { transform: translateY(-8px) rotate(0.5deg); }
  50% { transform: translateY(-15px) rotate(0deg); }
  75% { transform: translateY(-8px) rotate(-0.5deg); }
}

@keyframes scaleSpring {
  0% {
    opacity: 0;
    transform: scale(0.8);
  }
  60% {
    transform: scale(1.02);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes blurIn {
  0% {
    opacity: 0;
    filter: blur(20px);
    transform: scale(1.1);
  }
  100% {
    opacity: 1;
    filter: blur(0);
    transform: scale(1);
  }
}

@keyframes slideLeft {
  0% {
    opacity: 0;
    transform: translateX(-50px);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes slideRight {
  0% {
    opacity: 0;
    transform: translateX(50px);
  }
  100% {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes bounceSoft {
  0% { transform: scale(1); }
  30% { transform: scale(0.95); }
  50% { transform: scale(1.02); }
  70% { transform: scale(0.98); }
  100% { transform: scale(1); }
}

@keyframes shimmer {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}

@keyframes pulseGlow {
  0%, 100% {
    box-shadow: 0 0 20px rgba(103, 232, 249, 0.3);
  }
  50% {
    box-shadow: 0 0 40px rgba(103, 232, 249, 0.5);
  }
}

@keyframes typing {
  from { width: 0; }
  to { width: 100%; }
}

/* Magnetic button effect */
@keyframes magnetic {
  0% { transform: translate(0, 0); }
  25% { transform: translate(2px, -2px); }
  50% { transform: translate(-2px, 2px); }
  75% { transform: translate(2px, 2px); }
  100% { transform: translate(0, 0); }
}

/* ========================================================= */
/* 3. PWA NATIVE APP DETECTOR (CLIENT-SIDE) */
/* ========================================================= */

/* Secara bawaan (di Browser web), sembunyikan semua elemen khusus PWA/Aplikasi */
.show-in-pwa {
  display: none !important;
}

/* Jika dibuka dari ikon Aplikasi di Layar HP (Mode Standalone) */
@media (display-mode: standalone) {
  
  /* Sembunyikan elemen bawaan web (seperti Navbar Atas & Footer Portofolio Besar) */
  .hide-in-pwa {
    display: none !important;
  }
  
  /* Munculkan elemen khusus aplikasi (seperti Bottom Navigasi Mobile) */
  /* MENGGUNAKAN BLOCK AGAR TIDAK GEPENG */
  .show-in-pwa {
    display: block !important;
  }
}

/* ========================================================= */
/* 4. ANIMASI ESTETIK ALA APLIKASI NATIVE (CUSTOM TAILWIND) */
/* ========================================================= */

/* Animasi Slide Up saat Navigasi Terbuka (Slide dari Bawah) */
@keyframes navSlideUp {
  0% { transform: translateY(100%); opacity: 0; }
  100% { transform: translateY(0); opacity: 1; }
}

/* Animasi Pulse Glow (Denyut) untuk Tombol Tengah Utama */
@keyframes navPulseGlow {
  0% { transform: scale(1); opacity: 0.15; filter: blur(20px); }
  50% { transform: scale(1.1); opacity: 0.25; filter: blur(25px); }
  100% { transform: scale(1); opacity: 0.15; filter: blur(20px); }
}

/* Terapkan animasi di Tailwind utilities */
.animate-nav-slide-up {
  animation: navSlideUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) forwards;
}

.animate-nav-pulse {
  animation: navPulseGlow 2.5s ease-in-out infinite;
}
